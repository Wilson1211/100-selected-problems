#include <stdio.h>
#include <stdlib.h>
#include <string.h>


int cal(char *a, int a_len, char *b, int b_len) {
    
}

int longest_common_subsequence(char *a, char *b, char *result) {
    int i, j;
    int a_len = strlen(a);
    int b_len = strlen(b);

    
        


}