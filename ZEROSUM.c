#include <stdio.h>
#include <stdlib.h>
//#include <math.h>

#define   MAXSIZE   1000
#define   MIN(x, y) ((x<=y)?x:y)
#define   SWAP(x, y) {int *t; t = x;x = y;y = t;}


void sort(int [], int);

int zero_sum(int x[], int n){
    int i;
    int *y = ()malloc();
    for(i=0;i<n;i++){

    }
}
